package be.pxl.emailservice.selenide;

public class TestApplication {
    /*
    private static TestApplication INSTANCE = null;
    
    private final DocumentenboxProxy proxy;
    private boolean started = false;
    
    public TestApplication() {
        proxy = DocumentenboxProxy.getInstance();
    }
    
    public String documentenboxFrontendUrl() {
        return String.format("http://localhost:%s", port());
    }
    
    private int port() {
        return proxy.port();
    }
    
    public TestApplication start() {
        if (!started) {
            startProxy();
            started = true;
        }
        return this;
    }
    
    private void startProxy() {
        proxy.start();
    }
    
    public static TestApplication getInstance() {
        if (INSTANCE == null) {
            INSTANCE = new TestApplication();
        }
        return INSTANCE;
    }
    */
}
